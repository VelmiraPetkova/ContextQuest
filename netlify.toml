[build]
  publish = "src"
  functions = "netlify/functions"
  command = "npm install"

[functions]
  node_bundler = "nft"
  external_node_modules = ["@libsql/client"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200